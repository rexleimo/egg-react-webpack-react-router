import React, { PureComponent } from 'react';
import { withRouter } from 'react-router-dom';

class VoiceMaterial extends PureComponent {

    render() {
        return (
            <div>VoiceMaterial</div>
        );
    }
}

export default withRouter(VoiceMaterial);