import React, { Component } from 'react';

import {withRouter} from 'react-router-dom';

class AdminIndexPages extends Component {
    render() {
        return (
            <div>
                AdminIndexPages
            </div>
        );
    }
}
export default withRouter(AdminIndexPages);
